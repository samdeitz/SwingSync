# SwingSync
---
A hot reloader for Java Swing, allowing you to see your panel change from code changes in real time, without reruning it. Just pass it your main `JFrame`, `JPanel` and `JPanel` arguments, and you will have a hot reloading frame to do UI work on.